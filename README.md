# Sending SMS messages on MacOS using Python & AppleScript 

Steps:
1. `source commands.sh`
2. `text PHONE_NUMBER`
3. [OPTIONAL] To send a message to the last provided phone number, simply run: `text`

Then enter the message to send in the terminal.